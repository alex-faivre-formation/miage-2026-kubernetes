apiVersion: v1
kind: PersistentVolume
metadata:
  name: integration-pv
  labels:
    type: local
    app: postgresdb
spec:
  storageClassName: standard
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/data/integration-db"
