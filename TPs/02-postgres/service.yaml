apiVersion: v1
kind: Service
metadata:
  name: postgresdb
  namespace: postgres
spec:
  type: ClusterIP
  selector:
    app: postgresdb
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
