apiVersion: v1
kind: Pod
metadata:
  name: gpu-job
  namespace: default
spec:
  tolerations:
    - key: "gpu"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"
  containers:
    - name: gpu-job
      image: tensorflow/tensorflow:latest-gpu
